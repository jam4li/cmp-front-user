version: "3.9"
services:
  web-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "8080:8080"
    volumes:
      - .:/cmp-front-user
      - /cmp-front-user/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true

  web-prod:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./data/nginx/nginx.prod.conf:/etc/nginx/conf.d/default.conf
      - ./data/certs/cmeprice.net.pem:/etc/nginx/certs/cmeprice.net.pem
      - ./data/certs/cmeprice.net.key:/etc/nginx/certs/cmeprice.net.key
      - static_volume:/cmp_api_python/static
      - media_volume:/cmp_api_python/media
    networks:
      - my_network

volumes:
  static_volume:
    external: true
  media_volume:
    external: true

networks:
  my_network:
    external: true
